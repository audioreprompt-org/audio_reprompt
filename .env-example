# ==============================================
# CONFIGURACIÓN DEL ENTORNO (Audio Reprompt)
# Renombrar este archivo a .env para usarlo
# ==============================================

# --- 1. Configuración de Inferencia de Audio (RunPod) ---
# Requerido solo si MOCKED_RESPONSE=false.
# La URL debe apuntar al endpoint 'runsync' de tu worker en RunPod.
RUNPOD_API_KEY=rp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
RUNPOD_API_URL=https://api.runpod.ai/v2/xxxxxxx/runsync

# --- 2. Configuración del LLM (Moonshot AI) ---
# API Key para el modelo Kimi/Moonshot usado en el refinamiento de prompts.
MOONSHOT_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- 3. Base de Datos (PostgreSQL) ---
# Configuración para la conexión a la base de datos de vectores/metadatos.
# Si usas Docker Compose, DB_HOST suele ser el nombre del servicio de la BD.
# Si corres el backend localmente, usa 'localhost'.
DB_HOST=host.docker.internal
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=postgres

# --- 4. Modo de Ejecución ---
# true  = Modo Simulado (Dev): Devuelve audios pregrabados aleatorios (No gasta dinero).
# false = Modo Real (Prod): Llama a RunPod y Moonshot (Requiere API Keys válidas).
MOCKED_RESPONSE=true